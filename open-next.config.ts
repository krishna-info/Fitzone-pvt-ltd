import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // Add any OpenNext Cloudflare specific configuration here.
  // By default it handles the basic Cloudflare integration.
});
